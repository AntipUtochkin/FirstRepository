name: MyFirstCI

on: [push]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - name: Запускаю скрипт
      run: | 
           echo Send file
           @ftp -i -s:"%~f0"&GOTO:EOF
           open files.000webhost.com
           unrated-circuitry
           cWou@UfKhb)RftnDwjVS
           cd public_html
           pwd
           put FirstRepository/index.html
           disconnect
      shell: cmd
